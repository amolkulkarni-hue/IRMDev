<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Neg_FirstBillDate_Required</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  NOT(Bypass_Validation__c),
  ISCHANGED(StageName),
  ISPICKVAL(PRIORVALUE(StageName), &apos;Negotiation&apos;),
  ISPICKVAL(StageName, &apos;Booking Verification&apos;),
  OR(
    ISBLANK(IM_Booked_Date__c),
    IM_Booked_Date__c &lt;= TODAY(),
    IM_Booked_Date__c &lt;= CloseDate
  )
)</errorConditionFormula>
    <errorDisplayField>IM_Booked_Date__c</errorDisplayField>
    <errorMessage>First  Billing Date must be confirmed before moving this Opportunity from Negotiation to Booking Verification.</errorMessage>
</ValidationRule>
