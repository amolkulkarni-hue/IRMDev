<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Sol_BusinessCaseJustification_Required</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  NOT(Bypass_Validation__c),
  ISCHANGED(StageName),
  ISPICKVAL(PRIORVALUE(StageName), &apos;Solutioning&apos;),
  ISPICKVAL(StageName, &apos;Negotiation&apos;),
  OR(
    ISPICKVAL(Type, &apos;New&apos;),
    ISPICKVAL(Type, &apos;Cross-sell / Upsell&apos;),
    ISPICKVAL(Type, &apos;Framework&apos;)
  ),
  Business_Case_Justification__c = FALSE
)</errorConditionFormula>
    <errorMessage>Business Case Justified must be confirmed before moving this Opportunity from Solutioning to Negotiation for Type: New, Cross-sell / Upsell, or Framework.</errorMessage>
</ValidationRule>
