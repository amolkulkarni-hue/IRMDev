<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Create_RecordOnStage_1_Only_For_StdOpp</fullName>
    <active>false</active>
    <description>Created for IMNA Lightning Pilot to restrict stage opp can be created in.</description>
    <errorConditionFormula>IF(
AND(
AND(NOT($Setup.Metadata_Activation_Settings__c.Disable_VR__c &amp;&amp;
OR(ISBLANK($Setup.Metadata_Activation_Settings__c.SObject_API_Name__c), 
CONTAINS($Setup.Metadata_Activation_Settings__c.SObject_API_Name__c, &apos;Opportunity&apos;))),
RecordTypeId =&apos;01280000000BDei&apos;,
ISNEW(),
NOT(ISPICKVAL(StageName, &apos;1 - Qualification&apos;))
)),
true, false)</errorConditionFormula>
    <errorDisplayField>StageName</errorDisplayField>
    <errorMessage>New opportunities can only be created  as  Stage-1</errorMessage>
</ValidationRule>
