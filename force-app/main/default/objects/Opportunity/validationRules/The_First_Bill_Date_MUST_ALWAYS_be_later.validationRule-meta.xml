<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>The_First_Bill_Date_MUST_ALWAYS_be_later</fullName>
    <active>true</active>
    <description>The First Bill Date MUST ALWAYS be later than the Close date</description>
    <errorConditionFormula>AND(
AND(
NOT($Setup.Metadata_Activation_Settings__c.Disable_VR__c &amp;&amp; OR(ISBLANK($Setup.Metadata_Activation_Settings__c.SObject_API_Name__c),
CONTAINS($Setup.Metadata_Activation_Settings__c.SObject_API_Name__c, &apos;Opportunity&apos;))),
NOT($Setup.OpportunityAttribution__c.BypassValidation__c),
IM_Booked_Date__c &lt; CloseDate,
OR(
ISPICKVAL(StageName, &apos;1 - Qualification&apos;),
ISPICKVAL(StageName, &apos;2 - Solution Developed&apos;),
ISPICKVAL(StageName, &apos;3 - Solution Agreed&apos;),
ISPICKVAL(StageName, &apos;4 - Negotiate&apos;)
),
QTB_APIProcess__c = false
),
NOT($Permission.VR_Bypass_Permission)
)</errorConditionFormula>
    <errorDisplayField>IM_Booked_Date__c</errorDisplayField>
    <errorMessage>The First Bill Date must be always the same date or later than Close date</errorMessage>
</ValidationRule>
