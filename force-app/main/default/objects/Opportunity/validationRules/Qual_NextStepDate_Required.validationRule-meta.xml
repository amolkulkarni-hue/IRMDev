<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Qual_NextStepDate_Required</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  NOT(Bypass_Validation__c),
  ISCHANGED(StageName),
  ISPICKVAL(PRIORVALUE(StageName), &apos;Qualification&apos;),
  ISPICKVAL(StageName, &apos;Solutioning&apos;),
  OR(
    ISPICKVAL(Touch_Level__c, &apos;High Touch&apos;),
    ISPICKVAL(Touch_Level__c, &apos;Medium Touch&apos;)
  ),
  OR(
    ISBLANK(Next_Step_Date__c),
    Next_Step_Date__c &lt;= TODAY()
  )
)</errorConditionFormula>
    <errorDisplayField>Next_Step_Date__c</errorDisplayField>
    <errorMessage>Next Step Date is required for Medium Touch and High Touch opportunities before advancing from Qualification. Date must be in the future.</errorMessage>
</ValidationRule>
