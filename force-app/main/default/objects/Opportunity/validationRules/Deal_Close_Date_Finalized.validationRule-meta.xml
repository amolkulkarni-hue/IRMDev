<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Deal_Close_Date_Finalized</fullName>
    <active>true</active>
    <description>Close Date has been finalized following contract signature and cannot be modified.</description>
    <errorConditionFormula>AND(
    NOT($Permission.Bypass_Opportunity_Validation),
    Deal_Close_Date_Locked__c = TRUE,
    ISCHANGED(CloseDate),
    NOT(ISBLANK((Bypass_Deal_Date_Lock__c))
))</errorConditionFormula>
    <errorDisplayField>CloseDate</errorDisplayField>
    <errorMessage>Close Date has been finalized following contract signature and cannot be modified.</errorMessage>
</ValidationRule>
