<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Deal_Close_Date_Finalized</fullName>
    <active>true</active>
    <description>Deal Close Date has been finalized following contract signature and cannot be modified.</description>
    <errorConditionFormula>AND(
    NOT(Bypass_Validation__c),
    Deal_Close_Date_Locked__c = TRUE,
    ISCHANGED(CloseDate),
    NOT(ISBLANK((Bypass_Deal_Date_Lock__c))
))</errorConditionFormula>
    <errorDisplayField>CloseDate</errorDisplayField>
    <errorMessage>Deal Close Date has been finalized following contract signature and cannot be modified.</errorMessage>
</ValidationRule>
