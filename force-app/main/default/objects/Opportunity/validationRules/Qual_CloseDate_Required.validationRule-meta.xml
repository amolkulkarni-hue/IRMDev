<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Qual_CloseDate_Required</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
  NOT(Bypass_Validation__c),
  ISCHANGED(StageName),
  ISPICKVAL(PRIORVALUE(StageName), &apos;Qualification&apos;),
  ISPICKVAL(StageName, &apos;Solutioning&apos;),
  OR(
    ISBLANK(CloseDate),
    CloseDate &lt;= TODAY()
  )
)</errorConditionFormula>
    <errorDisplayField>CloseDate</errorDisplayField>
    <errorMessage>Deal Close Date is required and must be a future date before advancing from Qualification.</errorMessage>
</ValidationRule>
