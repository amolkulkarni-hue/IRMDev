<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Standard_Lead_Status_Change_NextStep_Req</fullName>
    <active>true</active>
    <errorConditionFormula>AND( NOT(ISNEW()), NOT($Permission.Bypass_Lead_Validation), 
				RecordType.DeveloperName = &quot;Standard_Lead&quot;, 
				ISCHANGED(Status), 
				ISPICKVAL(PRIORVALUE(Status), 
														&quot;Contacted&quot;), 
				OR( ISBLANK(Next_Step__c), ISBLANK(Next_Step_Date__c) ) 
			)</errorConditionFormula>
    <errorDisplayField>Next_Step__c</errorDisplayField>
    <errorMessage>Next Step and Next Step Date are required when moving a Lead from Contacted to another status.</errorMessage>
</ValidationRule>
