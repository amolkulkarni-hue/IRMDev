<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Validation_on_Lead_Conversion</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    NOT(ISNEW()),
    NOT($Permission.Bypass_Lead_Validation),
    RecordType.DeveloperName = &quot;Standard_Lead&quot;,
    ISCHANGED(Status),
    ISPICKVAL(Status, &quot;Converted&quot;),
    OR(
        ISBLANK(Street),
        ISBLANK(City),
        ISBLANK(State),
        ISBLANK(PostalCode),
        ISBLANK(Country)
    )
)</errorConditionFormula>
    <errorMessage>Complete Street, City, State, Postal Code, and Country before converting this Lead.</errorMessage>
</ValidationRule>
