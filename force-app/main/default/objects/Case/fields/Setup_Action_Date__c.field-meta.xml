<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Setup_Action_Date__c</fullName>
    <formula>IF(
OR(
	ISPICKVAL(NCSP_Service_line__c, &quot;RM&quot;),
	ISPICKVAL(NCSP_Service_line__c, &quot;RM/Shred&quot;)
),
IF(
	MONTH(DATEVALUE(Contract_Effective_Date__c)) = 1,
	DATE(YEAR(DATEVALUE(Contract_Effective_Date__c))-1,12,DAY(DATEVALUE(Contract_Effective_Date__c))),
	DATE(YEAR(DATEVALUE(Contract_Effective_Date__c)),MONTH(DATEVALUE(Contract_Effective_Date__c))-1,DAY(DATEVALUE(Contract_Effective_Date__c)))
),
DATEVALUE(Contract_Effective_Date__c)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Based on the Contract Effective Date and Service Line</inlineHelpText>
    <label>Setup Action Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
