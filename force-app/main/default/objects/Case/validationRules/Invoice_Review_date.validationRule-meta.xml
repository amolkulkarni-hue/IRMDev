<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Invoice_Review_date</fullName>
    <active>true</active>
    <description>Invoice Review date must be today&apos;s date. It cannot be in the Past or the Future.</description>
    <errorConditionFormula>((RecordType_Name__c = &quot;NCSP: SKP New Customer Setup&quot;)||
(RecordType_Name__c = &quot;NCSP: SKP Department Add Only&quot;)||
(RecordType_Name__c = &quot;NCSP: DP New Customer Setup&quot;))&amp;&amp;
OR(
	(
	NOT(ISCHANGED(Open_Tasks__c)) &amp;&amp;
	ISCHANGED(Invoice_Review_date__c ) &amp;&amp;
	NOT(ISBLANK(Invoice_Review_date__c )) &amp;&amp;
	DATEVALUE(Invoice_Review_date__c ) != TODAY() &amp;&amp;
	(
	  ISBLANK(PRIORVALUE(Invoice_Review_date__c )) ||
	  DATEVALUE(Invoice_Review_date__c ) != DATEVALUE(PRIORVALUE(Invoice_Review_date__c ))
	)
	),
    (	
	ISNEW() &amp;&amp;
	NOT(ISBLANK(Invoice_Review_date__c )) &amp;&amp;
	DATEVALUE(Invoice_Review_date__c ) != TODAY() 
	) 
)</errorConditionFormula>
    <errorDisplayField>Invoice_Review_date__c</errorDisplayField>
    <errorMessage>Invoice Review date must be today&apos;s date. It cannot be in the Past or the Future.</errorMessage>
</ValidationRule>
