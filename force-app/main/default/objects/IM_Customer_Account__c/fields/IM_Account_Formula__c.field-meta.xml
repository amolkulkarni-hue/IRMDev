<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IM_Account_Formula__c</fullName>
    <description>IF parent exists   
   THEN populate parent DUNS# if it is not blank, otherwise parent External Id
      ELSE otherwise leave blank</description>
    <externalId>false</externalId>
    <formula>/* If parent exists */
IF(LEN( IM_Account__c )&gt;0, 
   
   /*populate parent DUNS# if it is not blank, otherwise parent External Id*/
   IF(LEN(IM_Account__r.IM_DUNS_Nbr_Formula__c)&gt;0, IM_Account__r.IM_DUNS_Nbr_Formula__c, IM_Account__r.IM_External_ID__c), 

     /*otherwise leave blank*/
     &apos;&apos;)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Account (Formula)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
