<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Created_Date__c</fullName>
    <defaultValue>text(now())</defaultValue>
    <description>The most recent entry (as determined by this field) will always be used by the batch job.  You may create new entries for every batch job run, so that you have a record of what batch settings were used when.</description>
    <externalId>true</externalId>
    <inlineHelpText>The most recent entry (as determined by this field) will always be used by the batch job.  You may create new entries for every batch job run, so that you have a record of what batch settings were used when.</inlineHelpText>
    <label>Created Date</label>
    <length>255</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
