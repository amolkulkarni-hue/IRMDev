<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Query_Associate_Where__c</fullName>
    <defaultValue>&quot;&quot;</defaultValue>
    <description>In the SOQL query used by InboundStorageToBoBCoverage_Associate to select which billing records to process, this is the first of 3 text settings which are concatenated to form the WHERE clause</description>
    <inlineHelpText>In the SOQL query used by InboundStorageToBoBCoverage_Associate to select which billing records to process, this is the first of 3 text settings which are concatenated to form the WHERE clause</inlineHelpText>
    <label>Query Associate Where</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>TextArea</type>
</CustomField>
