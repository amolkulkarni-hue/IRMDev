/*****************************************************************************************************************************
Created By : Reena
Created Date : 17th Sept , 2010
Usages : Test coverage of PopulateSlaTargetDate Trigger.
*****************************************************************************************************************************/
@isTest
Private Class Test_PopulateSlaTargetDate {
    @isTest
    private static void testPopulateSlaTargetDate (){  
        /*ID caseRecordType ;
        Map<String,Schema.RecordTypeInfo> rtMapByName = RecordTypeSelection.getRecordTypeIds();        
        if(rtMapByName.containsKey('Issue'))
            caseRecordType = rtMapByName.get('Issue').getRecordTypeId();
        
        Case newCase = GenerateTestData.createCase(caseRecordType );    
        Task task = GenerateTestData.createTask();
        task.WhatID = newCase.ID;
        update task;   
        
        IM_Customer_Account__c custAcc = GenerateTestData.createCustomerAccount('102', 'test1');
        
        newcase.Customer_ID__c = custAcc.Id;
        update newcase;*/
    }
}